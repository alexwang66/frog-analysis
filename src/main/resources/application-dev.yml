spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/analysis?useUnicode=true&characterEncoding=utf-8
    username: root
    password: password
#    data: classpath:data.sql
#    initialization-mode: ALWAYS
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


logging:
  level:
    root: info
    com.alexwang.blog: debug
  file: /tmp/blog.log
